@model IEnumerable<ASM5.Models.Category>

<h1>Chủ đề</h1>
<ul>
    @foreach (var item in Model)
    {
        <li>@Html.ActionLink(item.CategoryName, "GetBookByCategory/", + item.CategoryId, "Book")</li>
    }
</ul>
